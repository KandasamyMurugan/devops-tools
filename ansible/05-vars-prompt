- name: Prompting Variables
  hosts: all
  vars_prompt: 
    - prompt: Enter URL
      name: URL
      private: yes
    - prompt: What is your username?
      name: username
      private: yes


  tasks:
    - name: Print URL
      debug:
        msg: "URL = {{URL}}"
        msg: 'Logging in as {{ username }}'
  
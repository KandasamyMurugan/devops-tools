- name: Prompting Variables
  hosts: all
  vars_prompt: 
    - prompt: Enter URL
      name: URL
      name: username
      private: yes

  tasks:
    - name: Print URL
      debug:
        msg: "URL = {{URL}}"
        msg: 'Logging in as {{ username }}'
  